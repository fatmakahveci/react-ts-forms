{
  "name": "react-ts-forms-examples",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "build": "npm --prefix apps/form-validation run build && npm --prefix apps/user-management run build",
    "typecheck": "npm --prefix apps/form-validation run typecheck && npm --prefix apps/user-management run typecheck",
    "lint": "npm --prefix apps/form-validation run lint && npm --prefix apps/user-management run lint && node apps/form-validation/node_modules/eslint/bin/eslint.js --config apps/form-validation/eslint.config.mjs packages/ui",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@testing-library/react": "16.3.3",
    "@types/react": "18.2.22",
    "@types/react-dom": "18.2.7",
    "jsdom": "30.1.1",
    "prettier": "^3.9.8",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "vitest": "5.0.0"
  }
}
